# AI 音乐个性化应用（天谱乐 API）

这是一个可快速启动的 MVP 骨架，包含：

- 用户端容器（`user-app/` 占位页）
- 后端 API（`backend/`，FastAPI）
- 后台管理系统容器（`admin-web/` 占位页）
- Docker Compose 部署模板（`deploy/`）

详细部署步骤请查看 `docs/deployment.md`，接口和推荐逻辑见 `docs/api.md`。
